apiVersion: v1
kind: Service
metadata:
  name: hello-world
  labels:
    app: hello-world
spec:
  selector:
    app: hello-world
  ports:
    - port: 8080          # Service port
      targetPort: 80      # Matches container port
      protocol: TCP
  type: ClusterIP
